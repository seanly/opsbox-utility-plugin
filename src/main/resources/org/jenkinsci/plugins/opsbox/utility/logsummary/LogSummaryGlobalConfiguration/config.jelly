<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:section title="Log Summary Configuration">

        <f:entry field="rulesConfig" title="${%Rules Configuration}" description="Configure log analysis rules in YAML format">
            <f:textarea clazz="yaml-config" default="---" codemirror-mode="yaml"
                        codemirror-config="mode: 'text/x-yaml', lineNumbers: true, theme: 'default'" />
        </f:entry>
    </f:section>
</j:jelly>